<!-- content heading -->
<% if (title) { %>
    <h1><%= title %></h1>
  <% } else {
    res.redirect('/')
  } %>
  
  <!-- message block -->
  <%# messages() %>
  
  <!-- inventory items display -->
<div class="vehicle-detail-container"><%- vehicleHtml %></div>